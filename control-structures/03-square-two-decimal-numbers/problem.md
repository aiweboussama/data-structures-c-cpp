# Square of Two Numbers

## Problem

Write a program to calculate the square of two numbers.

The numbers can have digits after the decimal point, and the result should be displayed with two digits after the decimal point.

## Example

Input:

```text
a = 2.50, b = 3.20
```

Output:

```text
Square of a = 6.25
Square of b = 10.24
```
