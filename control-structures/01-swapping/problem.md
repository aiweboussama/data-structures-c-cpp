# Swap Two Integers

## Problem

Write a program to swap two integers using two different methods:

1. Using a temporary variable
2. Without using a temporary variable

## Example

Input:

```
a = 5, b = 10
```

Output:

```
a = 10, b = 5
```
